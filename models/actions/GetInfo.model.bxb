action (GetInfo) {
  collect {
    input (currency) {
      type (Currency)
      min(Required) max(One)
    }
    
    input (targetCurrency) {
      type(TargetCurrency)
    }
  }
  output (Result)
  type (Calculation)
}
