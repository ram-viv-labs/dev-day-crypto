action (GetPrice) {
  collect {
    input (currency) {
      type (Currency)
      min (Required) max (One)
    }
    input (amount) {
      type (Amount)      
    }
    input (taretCurrency) {
      type (TargetCurrency)
    }
  }
  output (PriceResult)
  type (Calculation)
}
