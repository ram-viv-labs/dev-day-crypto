action (GetVolume) {
  collect {
    input (currency) {
      type (Currency)      
      min (Required) max (One)
    }
    input (targetCurrency) {
      type (TargetCurrency)
    }
  }
  output (PriceResult)
  type(Calculation)
}
